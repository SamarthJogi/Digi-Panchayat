# Server Configuration
server.port=8030
# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/DigiPanchayat?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Logging
logging.level.com.digipanchayat=DEBUG
logging.level.org.springframework.web=INFO

# Email Configuration (Gmail SMTP)
# NOTE: Replace with your Gmail credentials
# For Gmail, you need to enable "2-Step Verification" and create an "App Password"
# Instructions: https://support.google.com/accounts/answer/185833
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:digipanchaayat@gmail.com}
spring.mail.password=${MAIL_PASSWORD:knerambxgcvdyorh}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Application Properties	
app.otp.expiry.minutes=10
app.property.tax.rate.per.sqft=10
app.water.tax.fixed.annual=2000
